<Window x:Class="ScreenTimeGuardian.Agent.NotificationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="שומר זמן מסך"
        Width="380" SizeToContent="Height"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ResizeMode="NoResize"
        FlowDirection="RightToLeft">
    <Border Background="#111827" CornerRadius="12" Padding="20" BorderBrush="#374151" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="24" ShadowDepth="4" Opacity="0.5" />
        </Border.Effect>
        <StackPanel>
            <TextBlock Text="שומר זמן מסך" Foreground="#93C5FD" FontSize="12" Margin="0,0,0,10" />
            <TextBlock x:Name="TitleText" Foreground="White" FontSize="17" FontWeight="Bold" TextWrapping="Wrap" />
            <TextBlock x:Name="DetailText" Foreground="#D1D5DB" FontSize="14" TextWrapping="Wrap" Margin="0,8,0,0" />
            <TextBlock x:Name="CountdownText" Foreground="#FCA5A5" FontSize="22" FontWeight="Bold" Margin="0,12,0,0" />
            <Button Content="הבנתי" Click="DismissButton_OnClick" HorizontalAlignment="Left"
                    Margin="0,16,0,0" Padding="18,6" />
        </StackPanel>
    </Border>
</Window>
